# To-Do-App
A simple To-do app that can add, delete and mark tasks

This is a simple to-do app.

you can add tasks, delete them or mark them as completed, as you wish.

The tasks once you add them, they are saved in the localStorage of the browser.

Note: the background foto was picked from unSplash.

